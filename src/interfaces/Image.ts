export interface ImageAsset {
    _ref: string;
    _type: string;
}

export interface Image {
    _id: string;
    title: string;
    altText: string;
    description: string;
}

export interface Image {
    _id: string;
    title: string;
    altText: string;
    description: string;
}

export interface GalleryImage {
    image: Image;
}
